apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-used
spec:
  template:
    spec:
      containers:
      - name: app
        readinessProbe:
          httpGet:
            port: 8080
          periodSeconds: 30
        livenessProbe:
          httpGet:
            port: 8080
          periodSeconds: 30
